export type Store = {
    id: number,
    name: string,
    type:string,
    qttCurrent: number,
    qttBeloging: number
}

export type StoreShort = {
    id: number,
    name: string
}

export type StoreType = { 
    cod: number,
    name: string
}